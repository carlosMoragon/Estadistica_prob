
#Se pueden hacer de las 2 formas
#nombre = "Tino" # nombre <- "tino"
#print(nombre)
#Ejecutar = ctrl + sift + s
#Ejecutar linea a linea = ctrl + enter (En cada linea)

#---------------------------------

#calcular log de 1, 2, 3, 4, 5
#Las funciones están vectoriazadas
#my_vector = c(1,2,3, 4, 5) #c es de concatenar
#print(log(my_vector)) #devuelve un vector
#print(sum(my_vector)) #devuelve un int

#R representa todo en vectores
#my_long_vector = 1:100 #vector del 1 al 100
#print(log(my_long_vector))

#crear solo numeros pares
#seq(2, 10, by = 2) #del 2 al 10 en saltitos de 2
#seq(3, 100, by = 3) # multiplos de 3

#rep("gauss", 5) #repetir "gauss" 5 veces


#' estos son comentarios para comentar el codigo


#Para crear vectores random usamos las funciones
a = runif(3)
b = rnorm(3)
print(a)
print(b)


